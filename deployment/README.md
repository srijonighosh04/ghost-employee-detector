# Deployment — Vercel (frontend) + Render (backend)

Scaffolded, not implemented yet.

Planned structure:
- `vercel` — vercel.json / env config for the Next.js frontend
- `render` — render.yaml / service config for the FastAPI backend
- `ci-cd` — GitHub Actions workflows
