# Knowledge Graph — Neo4j

Scaffolded, not implemented yet.

Planned structure:
- `graph-models` — node/relationship definitions (Employee, Project, Document, Team, DEPENDS_ON, OWNS, AUTHORED, MEMBER_OF)
- `cypher-queries` — reusable Cypher queries (dependency lookups, resignation-impact traversal, risk aggregation)
- `scripts` — graph seeding / sync scripts from PostgreSQL
